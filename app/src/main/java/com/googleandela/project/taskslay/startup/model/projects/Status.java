package com.googleandela.project.taskslay.startup.model.projects;

public class Status {
  public static int ONLINE=1;
    public static int OFFLINE=0 ;
}
/*
mProjectViewModel = new ViewModelProvider(this).get(ProjectViewModel.class);

        mProjectViewModel.getAllProjects().observe(this, new Observer<List<Project>>() {
@Override
public void onChanged(@Nullable final List<Project> project) {
        // Update the cached copy of the words in the adapter.
        adapter.setUsers(users);
        }
        });
*/